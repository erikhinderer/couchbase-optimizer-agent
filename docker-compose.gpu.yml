# Overlay: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up
services:
  llm-service:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
